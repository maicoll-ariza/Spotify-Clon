import { OrdenListPipe } from './orden-list.pipe';

describe('OrdenListPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenListPipe();
    expect(pipe).toBeTruthy();
  });
});
